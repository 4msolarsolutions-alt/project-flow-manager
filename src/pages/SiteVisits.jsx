import { Layout } from "@/components/layout/Layout";

const SiteVisits = () => {
  return <Layout title="Site Visits"><p>Site Visits page - conversion in progress</p></Layout>;
};

export default SiteVisits;
