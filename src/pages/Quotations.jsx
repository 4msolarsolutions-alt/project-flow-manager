import { Layout } from "@/components/layout/Layout";

const Quotations = () => {
  return <Layout title="Quotations"><p>Quotations page - conversion in progress</p></Layout>;
};

export default Quotations;
