import { Layout } from "@/components/layout/Layout";

const QuotationPreview = () => {
  return <Layout title="Quotation Preview"><p>Quotation Preview - conversion in progress</p></Layout>;
};

export default QuotationPreview;
